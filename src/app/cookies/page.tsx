// src/app/cookies/page.tsx
export const metadata = { title: "Cookies — OneLine" };

export default function CookiesPage() {
  return (
    <main className="prose prose-invert mx-auto max-w-3xl px-6 py-10">
      <h1>Cookies</h1>
      <p>We use essential cookies to run the app. If we add analytics/ads, we will ask for your consent.</p>
    </main>
  );
}